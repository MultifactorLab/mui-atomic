name: Build Test
run-name: Build Test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: echo "Switch working directory"
      - run: ls -a && cd ./mui-atomic && ls -a

      - name: Install dependencies
        run: npm install --foreground-scripts

      - name: Run build
        run: npm run build
